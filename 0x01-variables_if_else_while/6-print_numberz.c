#include <stdio.h>

/**
 * main - Entry point
 * Description : prints all single digit numbers from 0 to 9
 * Return: 0 checks passed
*/
int main(void)
{
	int x;

	for (x = 48; x <= 57; x++)
	{
		putchar(x);
	}
	putchar('\n');
	return (0);
}
